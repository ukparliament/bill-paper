<table>
	<thead>
		<tr>
			<%= content_tag( 'td', 'Parliament' ) %>
			<%= content_tag( 'td', 'Session' ) %>
			<%= content_tag( 'td', 'Start date' ) %>
			<%= content_tag( 'td', 'End date' ) %>
		</tr>
	</thead>
	<tbody>
		<%= render :partial => 'session', :collection => @sessions %>
	</tbody>
</table>